import { EntryDurationPipe } from './entry-duration.pipe';

describe('EntryDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new EntryDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
